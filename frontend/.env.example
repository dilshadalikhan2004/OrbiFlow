# Frontend Environment Variables

# If your backend is hosted at https://orbiflow.onrender.com
VITE_API_URL=https://orbiflow.onrender.com/api/v1

# The WebSocket URL will be derived automatically from VITE_API_URL (it changes https to wss).
# You typically don't need to set VITE_WS_URL manually unless you want to override it.
